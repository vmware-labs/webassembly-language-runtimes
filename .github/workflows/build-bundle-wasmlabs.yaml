name: Build Lib Wasmlabs Bundle
on:
  push:
    # By specifying branches explicitly, we avoid this workflow from
    # running on tag push. We have a dedicated workflow to be ran when
    # a tag is pushed.
    branches:
      - main
    paths:
       - ".github/**"
       - "libs/bundle-wasmlabs/**"
       - "libs/*"
       - "scripts/**"
       - "Makefile*"
       - "*.sh"
  pull_request:
jobs:
  build-oniguruma:
    strategy:
      fail-fast: false
      matrix:
        include:
          - version: 0.1.0
    uses: ./.github/workflows/reusable-build-lib.yaml
    with:
      name: "libbundle-wasmlabs"
      version: ${{ matrix.version }}
      target-name: "bundle-wasmlabs"
      target-prefix: "v"
